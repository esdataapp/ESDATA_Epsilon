# Variables de entorno DASHBOARD (opcional)
# Copiar como .env.dashboard en la raíz o en esdata/dashboard si se desea.

# Periodo default cuando no se pasa --periodo
# DASHBOARD_PERIODO_DEFAULT=Sep25

# Puerto para streamlit (si quieres fijarlo)
# STREAMLIT_SERVER_PORT=8501
